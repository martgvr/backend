<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/head') %>
</head>
<body>
    <header>
        <%- include('../partials/header') %>
    </header>

    <h3>Mitad de la página</h3>
    
    <% if (render) { %>
        <ul>
            <% usuarios.forEach(usuario => { %>
                <li>
                    <%= usuario.nombre %> <%= usuario.apellido %>
                </li>    
            <% }) %>
        </ul>
    <% } %>

    <footer>
        <%- include('../partials/footer') %>
    </footer>
</body>
</html>